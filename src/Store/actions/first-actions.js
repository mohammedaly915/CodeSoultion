export const setSelectedService = (service) => ({
    type: 'SET_SELECTED_SERVICE',
    payload: service,
  });
  
  export const setWorks = (works) => ({
    type: 'SET_WORKS',
    payload: works,
  });